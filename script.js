const input = ;
const state = {
    toDos: [{
        description: "XXX",
        done: false,
    },
    {
        description: "XXX2",
        done: false,
    }]
}

// add new "your to dos"
/*document.appendChild()*/

// state anlegen
// input mit Button "add" verbinden
// wenn button geklickt: imput über "appendChild" in "new-to-dos"
// reset button (delete all new-to-dos)
// Filter (wenn "open": zeige open,...)
// filter: nur 1 Objekt gleichzeitig klickbar
// filter: default - "all" is activated
// to-dos als done markieren